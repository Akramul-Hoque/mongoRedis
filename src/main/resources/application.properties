########################################
# Application Info
########################################
spring.application.name=mongoRedis

########################################
# MongoDB Configuration
########################################
# Full connection string to your MongoDB Atlas cluster
spring.data.mongodb.uri=mongodb+srv://akramulhoque0093_db_user:yWKpCU7fPpHR45xR@cluster0.apcctuv.mongodb.net/mongoRedis?retryWrites=true&w=majority&appName=Cluster0

# Explicit database name to avoid ambiguity
spring.data.mongodb.database=mongoRedis

########################################
# Redis Configuration
########################################
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=60000
spring.cache.type=redis

########################################
# Server Configuration
########################################
server.port=8080

########################################
# Logging Configuration (Optional but Recommended)
########################################
# Show MongoDB queries in logs for debugging
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# Show Redis cache interactions in logs
logging.level.org.springframework.data.redis=DEBUG

# General Spring debugging (optional)
logging.level.org.springframework.web=INFO

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
